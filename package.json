{
  "name": "shimmer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "浮光 · Shimmer — a niche aesthetic creative image design tool. Landing prototype.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
